# Database — PostgreSQL
DATABASE_URL="postgresql://mecalc:your_password@localhost:5432/mecalc"

# Auth
JWT_SECRET="generate_with: node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""

# Socket.io server (runs on port 4000 separately)
NEXT_PUBLIC_SOCKET_URL="http://localhost:4000"

# App URL
NEXTAUTH_URL="http://localhost:3000"

# Optional: restrict CORS
ALLOWED_ORIGINS="http://localhost:3000"
